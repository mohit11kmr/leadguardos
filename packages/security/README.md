# `@leadguard/security`

Pure security contracts for URL safety, request identity, redaction, signatures, and authorization policy. Existing V5 implementations remain in `server/` until a later migration slice.
