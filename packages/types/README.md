# `@leadguard/types`

Platform-neutral V6 contracts only. This package has no React, Express, Prisma, Firebase, Node runtime, repository, or provider dependencies. Runtime validation belongs in `@leadguard/schemas`.
