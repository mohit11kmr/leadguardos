# `@leadguard/config`

`src/environment.ts` is a non-secret manifest of the environment variables already used by LeadGuard OS. It describes ownership and secrecy only; it does not load, validate, or expose values. Runtime validation remains in the V5 application until a later migration slice.
